{
  "name": "HashRange",
  "description": "String format of a 64bit HashRange. Hexadecimal values shorter than 16 digits in length are filled from the back with the same digit it ended with.",
  "definition": {
    "type": "string",
    "pattern": "/^[0-9a-z]{1,16}-[0-9a-z]{1,16}$/i"
  }
}
